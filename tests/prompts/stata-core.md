# Stata Core Prompt Fixtures

1. Write a Stata do-file that imports a CSV, standardizes string IDs, reshapes wide quarterly variables into long format, and saves a cleaned dataset without overwriting the raw file.
2. Explain why a `merge 1:1` failed in Stata when `_merge` shows unmatched observations and suggest a diagnostic workflow.
3. Build a batch-mode Stata regression workflow that runs `regress`, `margins`, and writes a log file suitable for terminal-based debugging.
