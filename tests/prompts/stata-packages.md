# Stata Packages Prompt Fixtures

1. Show how to estimate a two-way fixed effects model with `reghdfe`, clustered standard errors, and absorbed firm and year effects.
2. Compare `estout` and `outreg2` for exporting a set of regression tables and write a minimal example using one of them.
3. Write a small `rdrobust` example for a sharp regression discontinuity design and explain the minimum data checks before running it.
