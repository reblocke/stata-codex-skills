[project]
name = "stata-codex-skills"
version = "0.1.0"
description = "Builds local Codex skills for Stata from structured YAML and local help sources."
requires-python = ">=3.11"
dependencies = [
  "Jinja2>=3.1",
  "PyYAML>=6.0",
]

[tool.setuptools]
py-modules = []
