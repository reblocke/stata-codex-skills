# Moved route: {{ alias.from_route }}

This compatibility route is not a canonical reference.

{{ alias.task_intent }}

Load the `{{ alias.target_skill_name }}` skill, then open
`{{ alias.target_skill_name }}/{{ alias.target_route }}`.

Canonical reference: `{{ alias.target_slug }}`
