---
name: "{{ skill.name }}"
description: "{{ skill.description }}"
---

# {{ skill.heading }}

{{ skill.summary }}

## Working rules

{% for rule in skill.rules %}
- {{ rule }}
{% endfor %}
{% if skill.critical_gotchas %}

## Critical gotchas

{% for item in skill.critical_gotchas %}
- **{{ item.title }}**: {{ item.body }}
{% endfor %}
{% endif %}

## Choose the relevant reference

Use the existing code, explicit command or package choice, and requested outcome
to select a category. Open only its routing reference, then the needed technical
reference. Category indexes preserve detailed terms, commands, and aliases;
paths in those indexes are relative to this skill's root.

{% for section in sections %}
- [{{ section.name }}]({{ section.route_path }}): {{ section.guidance }}
{% endfor %}

For overlapping methods, use the available context first. Clarify only an
unresolved choice that changes scientific meaning, compatibility, dependencies,
or scope. A generic topic name alone does not require a question. Detailed
boundaries appear beside the affected routes in each category index.

{% if route_aliases %}
### Compatibility aliases

These routes exist only to redirect older references to their canonical skill.

| Legacy route | Aliases | Canonical route | Task intent |
|---|---|---|---|
{% for alias in route_aliases %}
| `{{ alias.from_route }}` | {{ alias.aliases | join(", ") }} | `{{ alias.target_skill_name }}/{{ alias.target_route }}` | {{ alias.task_intent }} |
{% endfor %}

{% endif %}
## Provenance

See `PROVENANCE.md` for the compact source and lock index.
