---
name: "{{ skill.name }}"
description: "{{ skill.description }}"
---

# {{ skill.heading }}

{{ skill.summary }}

## Working rules

{% for rule in skill.rules %}
- {{ rule }}
{% endfor %}
{% if skill.critical_gotchas %}

## Critical gotchas

{% for item in skill.critical_gotchas %}
- **{{ item.title }}**: {{ item.body }}
{% endfor %}
{% endif %}

## Routing by terms, names, and task intent

Curated terms and task intent control routing. Supported aliases and commands
help recognize explicit requests, but never override task intent or the
clarification boundaries that follow.

{% for section in sections %}
### {{ section.name }}

| Route | Curated terms | Supported aliases and commands | Task intent |
|---|---|---|---|
{% for entry in section.entries %}
| `{{ entry.route_path }}` | {{ entry.routing_terms | join(", ") }} | {{ (entry.aliases + entry.commands) | unique | join(", ") }} | {{ entry.trigger }} |
{% endfor %}

{% endfor %}
{% if routing_boundaries %}
### Terms that require clarification

Do not choose a route from these terms alone:

{% for boundary in routing_boundaries %}
- `{{ boundary.term }}`: {{ boundary.guidance }}
{% endfor %}

{% endif %}
{% if route_aliases %}
### Compatibility aliases

These routes exist only to redirect older references to their canonical skill.

| Legacy route | Aliases | Canonical route | Task intent |
|---|---|---|---|
{% for alias in route_aliases %}
| `{{ alias.from_route }}` | {{ alias.aliases | join(", ") }} | `{{ alias.target_skill_name }}/{{ alias.target_route }}` | {{ alias.task_intent }} |
{% endfor %}

{% endif %}
## Provenance

See `PROVENANCE.md` for the compact source and lock index.
