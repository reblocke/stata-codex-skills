# {{ entry.title }}

## When to use

{{ entry.trigger }}
{% if entry.preflight_commands %}

## Installation preflight (read-only)

Run these read-only checks before proposing installation:

```stata
{% for line in entry.preflight_commands %}
{{ line }}
{% endfor %}
```
{% endif %}
{% if entry.install_commands %}

## Optional: Install in an isolated directory

These commands change the Stata environment.
Run them only with user authorization.
Use only an isolated validation directory; never use the user's normal `PLUS`
or `PERSONAL` paths.

```stata
{% for line in entry.install_commands %}
{{ line }}
{% endfor %}
```
{% endif %}
{% if entry.commands %}

## Key commands and topics

Use only the commands that match the requested task:

{% for command in entry.commands %}
- `{{ command }}`
{% endfor %}
{% endif %}
{% if entry.syntax_patterns %}

## Syntax patterns

Use these task-specific patterns as a starting point:

```stata
{% for line in entry.syntax_patterns %}
{{ line }}
{% endfor %}
```
{% endif %}

## Common gotchas

{% for item in entry.gotchas %}
- {{ item }}
{% endfor %}

## Assumptions

{% for item in entry.assumptions %}
- {{ item }}
{% endfor %}

## Workflow

{% for item in entry.workflows %}
{{ loop.index }}. {{ item }}
{% endfor %}

## Validation

- Mode: `{{ entry.validation_mode }}`
- Case: {{ entry.validation_case }}

{% if entry.related_routes %}
## Related references

{% for item in entry.related_routes %}
- `{{ item.qualified_path }}`{{ item.instruction }}
{% endfor %}
{% endif %}
