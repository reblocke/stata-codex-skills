slug: coefplot
skill: packages
section: Graphics and Diagnostics
title: 'Coefplot: Visualizing Regression Coefficients'
trigger: Use when the user asks about coefplot or a related community-package workflow.
commands:
- coefplot
source_topics:
- coefplot
syntax_patterns: []
gotchas:
- Package syntax and dependencies vary across versions; verify the installed help file before finalizing
  code.
workflows:
- Confirm the package and any dependencies are installed on the active adopath before writing code that
  uses it.
- Pair the package command with a small reproducible example before folding it into a larger do-file.
validation_case: Run a small batch-mode example that exercises coefplot and inspect the resulting log.
related_refs:
- graphics
- linear-regression
install_commands:
- ssc install coefplot, replace
smoke_test: 'sysuse auto, clear

  reg price mpg weight

  coefplot'
provenance:
  local_help_topics:
  - coefplot
  local_help_files:
  - ado/base/e/estat_coefplot.sthlp
  upstream_files:
  - plugins/stata/skills/stata/packages/coefplot.md
  upstream_only: true
