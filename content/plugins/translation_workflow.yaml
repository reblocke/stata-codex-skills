slug: translation_workflow
skill: plugins
section: Translation Workflow
title: Translating Python/R Packages into Stata
trigger: Use when the task needs translating python/r packages into stata for a Stata plugin project.
commands:
- inventory
- wrap existing backend
- fidelity audit
source_topics:
- plugin
syntax_patterns: []
gotchas:
- A plugin crash terminates the Stata session, so treat every memory access and return code as high risk.
workflows:
- Plan the interface and the validation path before writing plugin code or wrapper ado-files.
- Use batch-mode Stata logs and compiler output together when debugging plugin failures.
validation_case: Start from an existing Python or R package and document the translation plan before coding.
related_refs:
- cpp_plugins
- testing_strategy
install_commands: []
smoke_test: null
provenance:
  local_help_topics:
  - plugin
  local_help_files:
  - ado/base/p/plugin.sthlp
  upstream_files:
  - plugins/stata-c-plugins/skills/stata-c-plugins/references/translation_workflow.md
  upstream_only: true
