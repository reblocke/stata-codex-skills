slug: performance_patterns
skill: plugins
section: Performance Patterns
title: C Plugin Performance Patterns
trigger: Use when the task needs c plugin performance patterns for a Stata plugin project.
commands:
- pthreads
- presort
- buffer reuse
source_topics:
- plugin
syntax_patterns: []
gotchas:
- A plugin crash terminates the Stata session, so treat every memory access and return code as high risk.
workflows:
- Plan the interface and the validation path before writing plugin code or wrapper ado-files.
- Use batch-mode Stata logs and compiler output together when debugging plugin failures.
validation_case: Compare a simple plugin baseline against a version that reduces repeated passes over
  the data.
related_refs:
- cpp_plugins
- testing_strategy
install_commands: []
smoke_test: null
provenance:
  local_help_topics:
  - plugin
  local_help_files:
  - ado/base/p/plugin.sthlp
  upstream_files:
  - plugins/stata-c-plugins/skills/stata-c-plugins/references/performance_patterns.md
  upstream_only: true
