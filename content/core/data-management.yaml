slug: data-management
skill: core
section: Data Operations
order: 130
title: Data Management in Stata
trigger: >-
  Use for generating or replacing variables, validating keys, merging or appending datasets, reshaping,
  collapsing, detecting duplicates, and constructing grouped variables with egen.
commands:
- generate
- replace
- isid
- duplicates
- merge
- joinby
- append
- reshape
- collapse
- egen
aliases: [merge data, join data, append data, reshape data, collapse data, duplicate keys, data cleaning]
routing_terms:
- merge 1:1 key
- merge m:1 key
- append using datasets
- reshape long to wide
- reshape wide to long
- collapse by group
- isid key validation
- duplicates report
source_topics:
- generate
- merge
- joinby
- duplicates
- isid
- append
- reshape
- collapse
- egen
syntax_patterns:
- isid patient_id visit_date
- merge m:1 site_id using "sites.dta"
- joinby firm using "contracts.dta"
- reshape long measure, i(id) j(time)
- collapse (mean) mean_price=price, by(group)
assumptions:
- Entity and observation keys have a documented meaning and expected cardinality in every input.
- Data transformations preserve the intended analysis unit or explicitly create a new documented unit.
gotchas:
- Verify key uniqueness with isid or duplicates before choosing 1:1, m:1, or 1:m merge cardinality.
- Missing key values can match other missing values and cannot identify real entities; audit them before merging.
- merge retains master-only and using-only rows by default; inspect _merge before filtering or dropping it.
- Avoid merge m:m because it performs positional matching within key groups rather than a relational join.
- collapse replaces the data in memory, so preserve or save a derived copy before destructive aggregation.
workflows:
- Determine the real key, assert its cardinality on both datasets, merge, tabulate _merge, and assert the intended disposition.
- Use joinby only when every within-key combination is substantively meaningful and verify the expected Cartesian row count.
- Reshape a small copy first, assert the i() identifier, and verify that suffixes or j() values map to the intended occasions.
validation_case: >-
  Build small master and using datasets, verify m:1 key cardinality, assert all merge results, and verify a
  deliberate joinby Cartesian count.
validation_mode: stata
related_refs:
- variables-operators
- workflow-best-practices
install_commands: []
smoke_test: |-
  clear all
  tempfile master groups
  input id group value
  1 1 10
  2 1 20
  3 2 30
  end
  isid id
  save `master'
  clear
  input group str4 label
  1 "one"
  2 "two"
  end
  isid group
  save `groups'
  use `master', clear
  merge m:1 group using `groups'
  assert _merge == 3
  drop _merge
  collapse (mean) mean_value=value, by(group)
  assert _N == 2
provenance:
  local_help_topics: [generate, merge, joinby, duplicates, isid, append, reshape, collapse, egen]
  local_help_globs: []
  local_help_files:
  - ado/base/g/generate.sthlp
  - ado/base/m/merge.sthlp
  - ado/base/j/joinby.sthlp
  - ado/base/d/duplicates.sthlp
  - ado/base/i/isid.sthlp
  - ado/base/a/append.sthlp
  - ado/base/r/reshape.sthlp
  - ado/base/c/collapse.sthlp
  - ado/base/e/egen.sthlp
  upstream_files:
  - plugins/stata/skills/stata/references/data-management.md
  upstream_only: false
  package_only: false
  reviewed: true
