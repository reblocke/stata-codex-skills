slug: advanced-programming
skill: core
section: Programming and Mata
order: 20
title: Reusable Stata Programs and Returned Results
trigger: >-
  Use for defining ado-style programs, parsing syntax, respecting if/in and weights, returning r() or e()
  results, managing program versions, preserving data, and writing estimation-command interfaces.
commands: [program define, syntax, marksample, return, ereturn, preserve, restore, version]
aliases: [ado programming, Stata program, rclass, eclass, syntax parser, returned results]
routing_terms:
- program define
- syntax varlist parser
- rclass program
- eclass estimation command
- marksample qualifier handling
- ereturn post results
- ado command development
source_topics: [program, syntax, return, ereturn]
syntax_patterns:
- program define command_name, rclass
- syntax varname(numeric) [if] [in] [, Detail]
- marksample touse, novarlist
- return scalar mean = r(mean)
- ereturn post b V, esample(touse)
assumptions:
- The program contract defines accepted arguments, data changes, returned results, and minimum Stata version.
- if/in qualifiers, weights, missingness, and current estimation state are handled deliberately.
gotchas:
- A program that ignores marksample can silently estimate on observations excluded by the caller.
- r() results are overwritten by many commands; copy values before issuing another r-class command.
- preserve must be paired with restore on every exit path, including handled errors.
- An eclass command must post conformable coefficient and variance matrices with stable names.
workflows:
- Write the public syntax and returned-results contract first, then implement a minimal valid path and explicit error checks.
- Test valid, boundary, and invalid inputs in clean sessions; verify data restoration and returned results after each path.
validation_case: >-
  Define a numeric-variable rclass command, parse its argument, return the mean, and verify the result against summarize.
validation_mode: stata
related_refs: [programming-basics, mata-programming]
install_commands: []
smoke_test: |-
  clear all
  capture program drop codex_mean
  program define codex_mean, rclass
      version 18
      syntax varname(numeric)
      quietly summarize `varlist'
      return scalar mean = r(mean)
  end
  sysuse auto, clear
  quietly summarize price
  scalar expected = r(mean)
  codex_mean price
  assert abs(r(mean) - expected) < 1e-10
provenance:
  local_help_topics: [program, syntax, return, ereturn]
  local_help_globs: []
  local_help_files:
  - ado/base/p/program.sthlp
  - ado/base/s/syntax.sthlp
  - ado/base/r/return.sthlp
  - ado/base/e/ereturn.sthlp
  upstream_files:
  - plugins/stata/skills/stata/references/advanced-programming.md
  upstream_only: false
  package_only: false
  reviewed: true
